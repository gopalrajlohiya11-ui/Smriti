import React from 'react';

/**
 * Cherry Blossom (🌸 / Phool) App Logo Icon
 * Restored authentic 5-petal pink flower matching the Smriti brand identity.
 */
export default function FoxtailOrchidIcon({ className = "w-6 h-6" }) {
  return (
    <span className={`inline-flex items-center justify-center text-xl sm:text-2xl leading-none select-none ${className}`} aria-label="Smriti Flower Logo" role="img">
      🌸
    </span>
  );
}
